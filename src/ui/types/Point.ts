export class Point {
  public x: number;
  public y: number;

  /**
   * Create a new point
   * @param {number} x
   * @param {number} y
   */
  public constructor(x: number, y: number) {
    this.x = x;
    this.y = y;
  }
}
